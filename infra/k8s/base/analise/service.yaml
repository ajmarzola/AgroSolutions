apiVersion: v1
kind: Service
metadata:
  name: analise
  labels:
    app.kubernetes.io/name: analise
    app.kubernetes.io/part-of: agrosolutions-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: analise
  ports:
    - name: http
      port: 80
      targetPort: http
