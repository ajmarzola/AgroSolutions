apiVersion: v1
kind: ConfigMap
metadata:
  name: analise-config
  labels:
    app.kubernetes.io/name: analise
    app.kubernetes.io/part-of: agrosolutions-platform
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  Logging__LogLevel__Default: "Information"
  # Exemplo de config de downstream:
  Identity__BaseUrl: "http://analise.default.svc.cluster.local"
