apiVersion: v1
kind: Service
metadata:
  name: usuarios
  labels:
    app.kubernetes.io/name: usuarios
    app.kubernetes.io/part-of: agrosolutions-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: usuarios
  ports:
    - name: http
      port: 80
      targetPort: http
